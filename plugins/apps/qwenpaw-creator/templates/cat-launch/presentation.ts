import cover from "./cover.jpg";

/** Presentation metadata for the built-in informal launch template. */
export const informalLaunchPresentation = {
  templateId: "informal_launch",
  cover,
} as const;
